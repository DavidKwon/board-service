server:
  port: 10000
  shutdown: graceful

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board?useUnicode=true&characterEncoding=utf8&verifyServerCertificate=false&useSSL=false&allowMultiQueries=true&autoReconnect=true&serverTimezone=Asia/Seoul
    username: root
    password: mop3936

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        ddl-auto: none
        show_sql: true
        use_sql_comments: false
        format_sql: true